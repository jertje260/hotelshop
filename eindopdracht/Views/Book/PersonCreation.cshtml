@model IEnumerable<eindopdracht.Models.Person>

@{
    ViewBag.Title = "PersonCreation";
}

<h2>PersonCreation</h2>
@foreach(var item in Model){
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <fieldset>
            <legend>Person</legend>

            <div class="editor-label">
                @Html.LabelFor(model => item.FirstName)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => item.FirstName)
                @Html.ValidationMessageFor(model => item.FirstName)
            </div>

            <div class="editor-label">
                @Html.LabelFor(model => item.InsertionName)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => item.InsertionName)
                @Html.ValidationMessageFor(model => item.InsertionName)
            </div>

            <div class="editor-label">
                @Html.LabelFor(model => item.LastName)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => item.LastName)
                @Html.ValidationMessageFor(model => item.LastName)
            </div>

            <div class="editor-label">
                @Html.LabelFor(model => item.Birthday)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => item.Birthday)
                @Html.ValidationMessageFor(model => item.Birthday)
            </div>

            <div class="editor-label">
                @Html.LabelFor(model => item.Gender)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => item.Gender)
                @Html.ValidationMessageFor(model => item.Gender)
            </div>
            
       
        </fieldset>
    }
         <p>
            <input type="submit" value="Create" />
        </p>
}

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
