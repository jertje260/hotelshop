@model eindopdracht.Models.Room

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Room</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.AmountOfPeople)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AmountOfPeople)
            @Html.ValidationMessageFor(model => model.AmountOfPeople)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MinimumPrice)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MinimumPrice)
            @Html.ValidationMessageFor(model => model.MinimumPrice)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
